//String, number, undefined, null, boolean, symbol?
const nome = 'Luiz'; //String
const nome1 = "Luiz"; //String
const nome2 = `Luiz`; //String
const num1 = 10; //Number
const num2 = 10.52 //Number
let nomeAluno; // undefined -> não aponta pra local nenhum na memoria
let sobrenomeAluno = null; //Nulo -> não aponta pra local nenhum na memoria
//null- pode ser trocado e retornar para null ( foi declarado que ela não deve apontar para nada na memoria) 
const aprovado = true;
const reprovado = false; // Boolean = true ou false (valor logico)

console.log(typeof nome); //TypeOf pra ver o tipo da string

